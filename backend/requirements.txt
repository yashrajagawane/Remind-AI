# ReMind AI — Backend runtime dependencies
# Python 3.11+ (developed/verified on 3.13). Pinned for reproducible builds.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# --- Data / ORM / migrations ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10        # PostgreSQL driver (Neon / local); SQLite used as dev fallback

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- AI / CV numerics (DeepFace + OpenCV are heavy; installed only where face
#     recognition runs — see Phase 4. The engine degrades gracefully without them.) ---
numpy>=2.1,<3
# deepface==0.0.93
# opencv-python-headless==4.10.0.84

# --- Media storage (Phase 3) ---
cloudinary==1.41.0

# --- Caching / rate-limit backend (Phase 2) ---
redis==5.2.1
