# ReMind AI — Backend development & test dependencies
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1                  # FastAPI TestClient transport + async API client

# --- Lint / format / types ---
ruff==0.8.4
black==24.10.0
mypy==1.14.0
